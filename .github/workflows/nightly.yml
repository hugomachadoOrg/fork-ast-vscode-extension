name: Nightly Release

permissions:
  id-token: write
  contents: write
  packages: write

on:
  push:
    branches:
      - main

jobs:
  nightly:
    uses: hugomachadoOrg/fork-ast-vscode-extension/.github/workflows/release.yml@main
    secrets: inherit
